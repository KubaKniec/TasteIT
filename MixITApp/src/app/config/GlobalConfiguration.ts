import {environment} from "../../environments/environment";

export const GlobalConfiguration = {
  API_URL: environment.API_URL,
  APP_VERSION: '1.0 DevBuild',
  APP_NAME: 'MixIT',
  ALLOW_ALL_DEVICES: true,
  SHOW_WARNINGS: false
}
